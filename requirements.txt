# Generation + metrics stack for the conversation-simulation project.
#
# IMPORTANT: install torch FIRST, matching the VM's CUDA version (see README).
#   e.g.  pip install torch --index-url https://download.pytorch.org/whl/cu118
# Then install the rest:

transformers>=4.40
accelerate>=0.30
bitsandbytes>=0.43
sentencepiece
protobuf       # required by transformers to convert the Llama/Vicuna SentencePiece tokenizer
tiktoken       # tokenizer-extractor fallback used by transformers 5.x

pandas
numpy
scipy
matplotlib

# Optional but recommended for analysis/social_metrics.py CAS/CED semantic embeddings.
# CED defaults to sentence-transformers (falls back to local TF-IDF if not installed) since
# TF-IDF measures topic-word overlap, not style. scikit-learn draws the CED 2-D projection
# plot (PCA); umap-learn is an optional alternative projection.
# sentence-transformers
# scikit-learn
# umap-learn

# ALIGN (Duran et al., 2019) for conceptual/syntactic/lexical alignment is installed
# SEPARATELY — it is finicky about dependency versions. Follow the install path used in
# the authors' OSF Colab notebooks (osf.io/zxwtr) and pin it in its own environment if
# it conflicts with the generation stack. See README "Phase 1".
